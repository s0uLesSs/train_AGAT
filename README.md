task5:

Был изменен KNN, скорректировал условие проверки бОльшего количества голосов.

Была переделана функция определения self.b в логистической регрессии в файле task_5. Теперь он не инициализируется нулями, а инициализируется функция вычисления логита среднего значения

task6:

Доделал оставшиеся задачки, добавил в несколько визуализацию. Сделал два класса бустинга и бэггинга, затем в последнем задании взял свою логит регрессию из таск5, бэггинг из склерн и свой бэггинг.
